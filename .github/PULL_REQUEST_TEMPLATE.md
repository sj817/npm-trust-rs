## What changed 改动内容

<!-- 一句话说明这个 PR 做了什么 -->

## Why 原因

<!-- 关联的 issue，或触发这次改动的具体场景 -->

## Checks 检查项

- [ ] `npm run typecheck` / `npm run build` / `npm test` pass 全部通过
- [ ] `npm run lint` / `npm run format:check` pass 全部通过
- [ ] Behavior changes are covered by tests 行为变化已有测试覆盖
- [ ] New user-facing strings go through `t(en, zh)` 新增界面文案已走双语
- [ ] `docs/api.md` updated if `src/registry/` changed 改动 `src/registry/` 时已同步文档
